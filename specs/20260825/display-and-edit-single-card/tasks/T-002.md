---
childId: T-002
parentId: 20260825-display-and-edit-single-card
title: "[T-002] 透過完整 Editor 即時更新合法設定"
assignedTo: null
iterationPath: null
state: done
---

## 任務描述

讓使用者透過具 accessible label 的 Editor 操作圖片、文字、價格、銷量、評論、四種 badges、accent color 與圓角，合法輸入立即更新同頁預覽且不 reload；只管理不持久化的 local draft。

依賴：T-001。

### 參考圖片

- `docs/assets/merchant-card-showroom/search-card-food.png` — 欄位與預覽對應基準。
- `docs/assets/merchant-card-showroom/search-card-health.png` — 保健圖片選項。
- `docs/assets/merchant-card-showroom/search-card-fashion.png` — 服飾圖片選項。

### 包含檔案

1. `components/product-card/ProductCardEditor.tsx` — 11 組欄位與四個 badge checkbox。
2. `components/product-card/product-card.validation.ts` — 數字、色彩與圓角驗證。
3. `components/product-card/ProductCardShowroom.tsx` — Editor 原始值與最後合法 preview config。
4. `components/product-card/ProductCard.tsx` — 即時呈現合法更新。
5. `app/globals.css` — Editor 與雙欄樣式。
6. `tests/e2e/merchant-card-showroom.e2e.ts` — AC-02、AC-03 案例。

### TDD 流程

🔴 Red：先新增 `AC-02: Editor 欄位完整` 與 `AC-03: 合法設定即時更新預覽`，執行 `npm --prefix /Users/chenzhiwen/Desktop/momo_shop run test:e2e -- --grep "AC-02|AC-03"`，確認因 Editor 尚未存在而失敗。

🟢 Green：實作受控 Editor、驗證函式與 Showroom local state，重跑 AC-02／AC-03 並確認測試 PASS。

🔵 Refactor：集中具型別的欄位更新 handler，重跑 E2E、TypeScript 與 ESLint 並確認仍 PASS。

## 驗收標準

- [ ] AC-02：11 組欄位皆有 accessible label，badge 包含速、折價券、登記、贈品；執行 `npm --prefix /Users/chenzhiwen/Desktop/momo_shop run test:e2e -- --grep "AC-02"`。
- [ ] AC-03：14 個控制項的合法變更不 reload 即反映；執行 `npm --prefix /Users/chenzhiwen/Desktop/momo_shop run test:e2e -- --grep "AC-03"`。
- [ ] 不加入 persistence、Save、Discard 或 Reset；執行 `rg -n "persist|localStorage|Save|Discard|Reset" components/product-card`，預期無相關實作。
- [ ] TypeScript 與 ESLint 通過。
