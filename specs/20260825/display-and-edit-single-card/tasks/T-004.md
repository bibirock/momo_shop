---
childId: T-004
parentId: 20260825-display-and-edit-single-card
title: "[T-004] 完成 390px 響應式操作與瀏覽器錯誤驗收"
assignedTo: null
iterationPath: null
state: todo
---

## 任務描述

讓使用者在 390px viewport 檢視並操作完整 Showroom 時，預覽與 Editor 依序堆疊、頁面沒有水平捲動，且過程不產生 console error 或 uncaught page error。依使用者修正，不驗收純鍵盤巡覽與 Tab 順序。

依賴：T-003。

### 參考圖片

無；本任務驗收 Showroom 響應式布局。

### 包含檔案

1. `components/product-card/ProductCardShowroom.tsx` — 窄螢幕 DOM 與顯示順序。
2. `components/product-card/ProductCardEditor.tsx` — 390px 控制項操作。
3. `app/globals.css` — 單欄堆疊、最小寬度與 overflow 防護。
4. `tests/e2e/merchant-card-showroom.e2e.ts` — 390px、操作、scrollWidth 與錯誤監聽。

### TDD 流程

🔴 Red：先新增 `AC-05: 390px 可操作且無水平溢出`，執行 `npm --prefix /Users/chenzhiwen/Desktop/momo_shop run test:e2e -- --grep "AC-05"`，確認因響應式版面缺口而失敗。

🟢 Green：完成窄螢幕堆疊、寬度與 overflow 限制，重跑 AC-05 並確認測試 PASS。

🔵 Refactor：移除重複 breakpoint 規則並確認桌面未回歸，重跑完整驗證束並確認測試與驗證全部 PASS。

## 驗收標準

- [ ] AC-05：390px 下預覽與 Editor 依序顯示且代表性欄位可操作；執行 `npm --prefix /Users/chenzhiwen/Desktop/momo_shop run test:e2e -- --grep "AC-05"`。
- [ ] AC-05：`document.scrollWidth <= window.innerWidth`；執行同一 AC-05 案例。
- [ ] AC-05：console error 與 uncaught page error 數量均為 0；執行同一 AC-05 案例。
- [ ] 完整驗證束通過：ESLint、TypeScript、Next.js production build 與 Playwright E2E。
