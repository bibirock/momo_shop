---
childId: T-002
parentId: 20260825-load-card-from-sample-html
title: "[T-002] Embed route 顯示 demo-food 與錯誤卡"
assignedTo: null
iterationPath: null
state: todo
---

## 任務描述

新增 `/embed/[cardId]`，以既有 ProductCard renderer 呈現 demo-food 的 persisted/default config；未知 cardId 顯示指定錯誤，不 fallback。

包含檔案：

- `app/embed/[cardId]/page.tsx` — route 參數判斷與頁面。
- `components/product-card/ProductCard.tsx` — 共用 renderer 的嵌入調整。
- `app/globals.css` — embed 卡片與錯誤訊息樣式。
- `tests/e2e/merchant-card-embed.e2e.ts` — route/default/unknown card 案例。

TDD：Red 寫 `embed uses persisted config`、`unknown card shows explicit error`、`invalid persisted payload falls back` 測試並執行 `npm --prefix /Users/chenzhiwen/Desktop/momo_shop run test:e2e -- --grep "embed uses persisted|unknown card|invalid persisted"` 確認失敗；Green 實作並確認 PASS；Refactor 後重跑確認 PASS。

## 驗收標準

- [ ] `/embed/demo-food` 顯示完整商品卡；驗收指令：`npm --prefix /Users/chenzhiwen/Desktop/momo_shop run test:e2e -- --grep "embed uses persisted"`。
- [ ] unknown card 顯示 `找不到商品：<cardId>` 且不 fallback；驗收指令：`npm --prefix /Users/chenzhiwen/Desktop/momo_shop run test:e2e -- --grep "unknown card"`。
- [ ] lint / typecheck 通過；驗收指令：`npm --prefix /Users/chenzhiwen/Desktop/momo_shop run lint && /Users/chenzhiwen/Desktop/momo_shop/node_modules/.bin/tsc -p /Users/chenzhiwen/Desktop/momo_shop/tsconfig.json --noEmit --incremental false`。
