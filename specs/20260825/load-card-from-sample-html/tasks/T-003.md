---
childId: T-003
parentId: 20260825-load-card-from-sample-html
title: "[T-003] Sample loader mount/destroy 嵌入流程"
assignedTo: null
iterationPath: null
state: done
---

## 任務描述

提供 `/sample.html` 與 `/momo-card.js`。Loader 由自身 script URL 推導 origin，支援 target/cardId 驗證、單一 iframe、lazy/title/responsive 屬性與冪等 destroy。

包含檔案：

- `public/momo-card.js` — 自包含瀏覽器 loader、mount 與 destroy handle。
- `public/sample.html` — mount 程式碼範例與實際 demo-food instance。
- `app/globals.css` — sample embed 容器樣式。
- `tests/e2e/merchant-card-embed.e2e.ts` — loader、origin、lifecycle 與邊界案例。

TDD：Red 寫 `sample page mounts demo-food`、`loader derives script origin`、`destroy removes iframe`、`invalid target shows explicit error` 測試並執行 `npm --prefix /Users/chenzhiwen/Desktop/momo_shop run test:e2e -- --grep "sample page mounts|derives script origin|destroy removes|invalid target"` 確認失敗；Green 實作並確認 PASS；Refactor 後重跑確認 PASS。

## 驗收標準

- [ ] Sample 程式碼與實際 mount 一致且只有一個 demo-food instance；驗收指令：`npm --prefix /Users/chenzhiwen/Desktop/momo_shop run test:e2e -- --grep "sample page mounts"`。
- [ ] iframe URL 使用 script origin，具 meaningful title、`loading="lazy"` 與 responsive width；驗收指令：`npm --prefix /Users/chenzhiwen/Desktop/momo_shop run test:e2e -- --grep "derives script origin"`。
- [ ] destroy 移除 iframe，錯誤 target/cardId 顯示指定訊息；驗收指令：`npm --prefix /Users/chenzhiwen/Desktop/momo_shop run test:e2e -- --grep "destroy removes|invalid target"`。
- [ ] lint / typecheck / build 通過；驗收指令：`npm --prefix /Users/chenzhiwen/Desktop/momo_shop run lint && /Users/chenzhiwen/Desktop/momo_shop/node_modules/.bin/tsc -p /Users/chenzhiwen/Desktop/momo_shop/tsconfig.json --noEmit --incremental false && npm --prefix /Users/chenzhiwen/Desktop/momo_shop run build`。
