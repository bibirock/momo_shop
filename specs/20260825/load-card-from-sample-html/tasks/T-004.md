---
childId: T-004
parentId: 20260825-load-card-from-sample-html
title: "[T-004] Showroom 儲存設定並由 Sample 還原"
assignedTo: null
iterationPath: null
state: todo
---

## 任務描述

在 Showroom 增加明確儲存動作，將成功驗證的 demo-food config 寫入 persisted store；重新載入 Sample/Embed 顯示最後成功儲存值，未儲存 draft 不外洩。

包含檔案：

- `components/product-card/ProductCardShowroom.tsx` — draft、save 狀態與成功回饋。
- `components/product-card/product-card-store.ts` — canonical config 寫入與讀取。
- `tests/e2e/merchant-card-embed.e2e.ts` — save → reload 完整流程。

TDD：Red 寫 `showroom save survives sample reload` 與 `unsaved draft is not embedded`，執行 `npm --prefix /Users/chenzhiwen/Desktop/momo_shop run test:e2e -- --grep "survives sample reload|unsaved draft"` 確認失敗；Green 實作並確認 PASS；Refactor 後重跑確認 PASS。

## 驗收標準

- [ ] 儲存後 reload Sample/Embed 顯示完整設定；驗收指令：`npm --prefix /Users/chenzhiwen/Desktop/momo_shop run test:e2e -- --grep "survives sample reload"`。
- [ ] 未儲存 draft 不出現在 Sample，無 persisted data 時顯示 defaults；驗收指令：`npm --prefix /Users/chenzhiwen/Desktop/momo_shop run test:e2e -- --grep "unsaved draft"`。
- [ ] 既有 Showroom 回歸測試通過；驗收指令：`npm --prefix /Users/chenzhiwen/Desktop/momo_shop run test:e2e -- --grep "Showroom|AC-0"`。
